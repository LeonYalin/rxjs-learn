import { delimeterMsg, logF } from "./utils";

function creatingCustomOperators() {
  
}

export default function createCustomOperators() {
  delimeterMsg('CREATING CUSTOM OPERATORS');
  logF(creatingCustomOperators);
}